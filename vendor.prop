# Bluetooth
bluetooth.device.default_name=moto edge 30 neo

# Camera
persist.vendor.camera.physical.num=3

# Motorola
ro.mot.build.customerid=global

# NFC
persist.vendor.nfc.config_secfile_name=libnfc-sec-nci.conf

# Surfaceflinger
ro.sf.lcd_density=400
